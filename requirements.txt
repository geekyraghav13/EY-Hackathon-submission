# Minimal requirements for Render deployment (no C++ compilation needed)
flask==3.0.0
requests==2.31.0
beautifulsoup4==4.12.2
faker==21.0.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Removed for deployment (only needed for local diagram generation):
# pandas==2.2.0 - C++ compilation issues on Python 3.13
# numpy==1.26.4 - Not needed in production
# matplotlib==3.8.2 - Diagrams already generated locally
# selenium==4.16.0 - Not used in core app
# openai==1.6.1 - Not needed for demo (can add back if needed)
# reportlab==4.0.7 - Not needed in core app
# pillow==10.1.0 - Not needed in core app
